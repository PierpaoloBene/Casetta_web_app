name: Deploy Next.js site to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

# Disable GitHub Pages deploy - now handled by Vercel
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Note
        run: echo "Deployment is now handled by Vercel. Push to main and Vercel will auto-deploy."
